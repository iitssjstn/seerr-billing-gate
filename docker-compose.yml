services:
  billing-gate:
    image: ghcr.io/iitssjstn/seerr-billing-gate:latest
    container_name: seerr-billing-gate
    restart: unless-stopped
    ports:
      - "3010:3000"
    env_file:
      - path: .env
        required: false
    volumes:
      - ./data:/app/data
